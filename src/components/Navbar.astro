<style>
  .navbar-blur {
    -webkit-backdrop-filter: blur(20px); /* Fixes blur for Safari */
    backdrop-filter: blur(20px);
  }
</style>

<nav
  class="w-full z-50 flex-col pt-6 pb-7 top-0 w-full sticky navbar-blur gap-2 flex justify-between font-kai items-center lt-xs:px-4 px-8 mx-auto"
>
  <a
    href="/"
    class="uppercase text-black dark:text-white font-bold text-xl md:text-3xl"
    >Max DeMaio</a
  >
  <!-- spacer -->
  <div></div>
  <div class="flex gap-6">
    <a
      href="https://soundcloud.com/maxdemaio"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Soundcloud"
      class="cursor-pointer transition-color duration-200 ease-in-out hover:text-black dark:hover:text-white text-base grid items-center justify-center"
    >
      <div class="i-mdi-soundcloud"></div>
    </a>
    <a
      href="https://instagram.com/maxdemaio"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Instagram"
      class="cursor-pointer transition-color duration-200 ease-in-out hover:text-black dark:hover:text-white text-base grid items-center justify-center"
    >
      <div class="i-mdi-instagram"></div>
    </a>
    <a
      href="https://github.com/maxdemaio/music"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="GitHub"
      class="cursor-pointer transition-color duration-200 ease-in-out hover:text-black dark:hover:text-white text-base grid items-center justify-center"
    >
    <div class="i-mdi-github-face"></div>
    </a>
    <a
      href="mailto:maxwell.demaio@gmail.com"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Email"
      class="cursor-pointer transition-color duration-200 ease-in-out hover:text-black dark:hover:text-white text-base grid items-center justify-center"
    >
      <div class="i-mdi-email"></div>
    </a>
    <button
      title="Theme"
      id="theme-button"
      aria-label="Theme"
      class="cursor-pointer transition-color duration-200 ease-in-out hover:text-black dark:hover:text-white text-base grid items-center justify-center"
    >
      <div class="i-tabler-sun-filled dark:i-tabler-moon-stars"></div>
    </button>
  </div>
</nav>

<script is:inline>
  const theme = (() => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    if (window.matchMedia("(prefers-color-scheme: dark)").matches) {
      return "dark";
    }
    return "light";
  })();

  if (theme === "light") {
    document.documentElement.classList.remove("dark");
  } else {
    document.documentElement.classList.add("dark");
  }

  window.localStorage.setItem("theme", theme);

  const handleToggleClick = () => {
    const element = document.documentElement;
    element.classList.toggle("dark");

    const isDark = element.classList.contains("dark");
    localStorage.setItem("theme", isDark ? "dark" : "light");
  };

  const themeButton = document.getElementById("theme-button");

  if (themeButton) {
    themeButton.addEventListener("click", handleToggleClick);
  }
</script>
